{
  "name": "webmaster-pro",
  "version": "1.0.0",
  "description": "WebMaster Pro - AI-Powered Website Builder Platform",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev",
    "setup": "cd backend && npm install && npm run setup",
    "test": "cd backend && npm test",
    "install-backend": "cd backend && npm install",
    "migrate": "cd backend && npm run migrate",
    "deploy": "cd backend && npm run deploy",
    "build": "echo 'No build step needed for static files'",
    "lint": "echo 'Linting not configured'",
    "postinstall": "cd backend && npm install"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/webmaster-pro.git"
  },
  "keywords": [
    "webmaster",
    "website-builder",
    "ai",
    "openai",
    "claude",
    "visual-editor",
    "templates",
    "business-websites"
  ],
  "author": "WebMaster Pro Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "workspaces": [
    "backend"
  ],
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {},
  "directories": {
    "public": "./public",
    "websites": "./websites",
    "assets": "./assets",
    "backend": "./backend"
  }
}
